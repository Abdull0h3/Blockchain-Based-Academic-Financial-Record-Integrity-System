# Example environment variables for the Next.js frontend (server-side).
# Copy this file to ".env.local" in the frontend folder and fill the values.
# Never commit the real .env.local file.

# Same PRIVATE_KEY used by the Hardhat deployer/admin account.
PRIVATE_KEY= 0xac0974bec39a17e36ba4a6b4d238ff944bacb478cbed5efcae784d7bf4f2ff80

# Address of the deployed AcademicRecords contract.
CONTRACT_ADDRESS= 0x5FbDB2315678afecb367f032d93F642f64180aa3

# SQL Server credentials for the academic_system database.
# You can manage this database using SQL Server Management Studio (SSMS).
# If you use a named instance (e.g. SQLEXPRESS), set:
#   DB_HOST=localhost
#   DB_INSTANCE=SQLEXPRESS
# and you can omit DB_PORT.
DB_HOST=localhost
DB_PORT=1433
DB_INSTANCE= 
DB_USER=admin
DB_PASSWORD=admin
DB_NAME=academic_system

# Optional: custom RPC URL for the local Hardhat node.
RPC_URL=http://127.0.0.1:8545

